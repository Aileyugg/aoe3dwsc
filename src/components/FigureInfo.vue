<script setup>
import { computed, ref } from 'vue';
import { locateIcon } from '../modules';

const props = defineProps({
  title: String,
  // data: Object,
  // type: String,
  // get: Function,
  // compute: Function,
  // // order: {
  // //   type: Array,
  // //   default: [],
  // // },
  // order: Array,
})
</script>